# Shared layer

Reusable **presentational** UI. No `ProductService`, HTTP, or repository calls.

- `layout/` — `SiteHeader`, `SiteFooter` (shell)
- `ui/` — `Button`, `ProductCard`, `SectionHeading`, `Skeleton`

See [`../ARCHITECTURE.md`](../ARCHITECTURE.md) for dependency rules.
